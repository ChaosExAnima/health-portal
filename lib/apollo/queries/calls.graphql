query CallsIndex($offset: Int!) {
	getCalls(offset: $offset) {
		totalCount
		calls {
			id
			slug
			date
			rep
			provider {
				__typename
				name
				slug
			}
		}
	}
}

query Call( $slug: String! ) {
	call(slug: $slug) {
		id
		slug
		date
		provider {
			id
			name
			slug
		}
	}
}
