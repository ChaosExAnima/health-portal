query CallsIndex($offset: Int!) {
	getCalls(offset: $offset) {
		totalCount
		calls {
			id
			slug
			date
			reps
			notes {
				description
			}
			providers {
				__typename
				name
				slug
			}
		}
	}
}

query Call( $slug: String! ) {
	call(slug: $slug) {
		id
		slug
		date
		providers {
			id
			name
			slug
		}
	}
}

mutation CallNew( $call: CallInput! ) {
	newCall(call: $call) {
		success
		call {
			id
		}
	}
}
